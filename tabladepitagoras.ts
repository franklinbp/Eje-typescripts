let numero : number = parseInt(prompt("Ingrese el tamano de las tablas"));

//utilizamos for para hacer la tabla de pitagoras

for (let i : number = 1; i <=numero; i++){
    let fila; string =""; 
    for (let i ; number = 1; i<= numer; i++){
        fila += (i * j ) + "lt";
    }
console.log(fila.trim());//visualizmamos la fola completa 
}